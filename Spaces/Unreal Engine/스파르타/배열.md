# 배열

- 배열은 다양한 변수값을 담을 수 있는 변수

| 타입  | 변수명 | []  |
| --- | --- | --- |

- 배열을 통째로 대입하는 것은 불가능(컴파일 오류 발생)
- 배열의 이름은 배열의 시작 주소를 가지고 있으므로 주소값을 변경할 수 없는 상수 포인터다

```
buf[0] == *buf
buf[k] == *(buf+k)
```


